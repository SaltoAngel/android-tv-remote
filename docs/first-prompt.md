create an android tv remote control app for gnome (installed via flatpak) that:
- scans network for IPs like NetPeek
- tries to connect to devices with "adb connect DEVICE_IP:5555" command (can we bundle adb in flatpak package?)
- shows a remote controll UI